import React, { FC } from 'react';
import Header from '../../../components/Header/Header';

const Home: FC = () => {

    return (
        <div>
            <Header />
            <h1>HOME</h1>
        </div>
    );
};

export default Home;